![image-20251010164902674](C:\Users\LGX_MATE_BOOK\AppData\Roaming\Typora\typora-user-images\image-20251010164902674.png)

### 离散1-形式详解

离散1-形式是**定义在网格边上的实值函数**。想象一个三角网格，给每条边赋予一个实数（可以理解为沿该边的“流量”或“积分”），这就得到了一个离散1-形式。

其数学基础是**离散外微积分（DEC）**。DEC将连续微分几何的概念（如微分形式、外导数、霍奇星算子）移植到离散网格上。关键在于**离散外导数 d**：它将k-形式变为(k+1)-形式。对于0-形式（顶点函数）ƒ，**dƒ 是一个1-形式**，其在边 (i,j) 上的值就是 ƒ(j) - ƒ(i)，这其实就是梯度。

1-形式 ω 称为**闭的**，如果 dω=0。对于1-形式，这在三角网格上意味着**每个三角形上，三条边的1-形式值之和为0**。1-形式 ω 称为**恰当的**，如果存在0-形式 ƒ 使得 ω = dƒ。

并非所有闭形式都是恰当的，这引出了**上同调**的概念。**调和1-形式**则是满足 dω=0 且 δω=0（余导数为零）的形式，它们对应于网格的拓扑特征。

### 🛠️ 实现步骤与代码框架

实现基于离散1-形式的网格参数化，核心步骤可概括如下：

1. **计算生成元**：找到网格的**拓扑生成元**（即一组能捕捉网格“洞”的环）。
2. **计算调和1-形式**：为每个生成元计算一个**调和1-形式**。
3. **指定目标上同调类**：根据需要，设定参数化边界在参数域中的形状（如矩形、圆盘），这决定了目标上同调类。
4. **构造参数化函数**：通过**霍奇分解**，结合调和1-形式和目标上同调类，构造参数化函数 Φ: M → D (D是参数域)。

### 核心实现要点

1. **离散微分算子**：正确构建 **`d0`** (梯度算子)和 **`d1`** (旋度算子)是关键。`d0` 的大小是 `|E| x |V|`，`d1` 的大小是 `|F| x |E|`。
2. **调和形式计算**：调和1-形式满足 **Δω = (dδ + δd)ω = 0**。这需要求解一个大型稀疏线性系统。
3. **霍奇分解实现**：可通过求解 **泊松方程** 来得到恰当分量，然后通过投影得到调和分量。
4. **线性求解器**：由于系统矩阵通常是大型、稀疏、正定的，使用 **Cholesky 分解**（如Eigen的SimplicialLDLT）或**共轭梯度法** 是高效的选择。