---
layout: post
title: "共形参数化介绍"
category: Parameterization
categories: ["Parameterization", "Parameterization-ConformalMapping"]
---

共形几何**只考虑角度**，而不需要考虑长度或距离的度量，这种能保持局部角度的映射称为**保角映射**（Angle Preserving），在高等数学理论中称这类保持角度的映射为**共形映射 (Conformal Mapping)**。

<img src="https://lgximgs.oss-cn-beijing.aliyuncs.com/images/image-20260608173327816.png" alt="image-20260608173327816" style="zoom:50%;" />

墨卡托投影是共形映射的典型例子：面积虽被扭曲，局部形状与角度得以保持——对导航而言，角度保真远比面积保真重要。

<img src="https://lgximgs.oss-cn-beijing.aliyuncs.com/images/image-20251013174637718.png" alt="image-20251013174637718" style="zoom:50%;" />

## 相似图形视角

从保持角度的视角来看，共形映射即是我们初中所学的相似三角形

<img src="https://lgximgs.oss-cn-beijing.aliyuncs.com/images/image-20260529124336866.png" style="zoom:50%;" />

直接保角虽然简单直观，但是通过限制角度的方式会导致变换太过**刚性(rigid)**，为了能尽可能保角，

![](https://lgximgs.oss-cn-beijing.aliyuncs.com/images/image-20260605072918934.png)

另外数值计算上也将导出一个带非线性约束条件,难以计算

$$
\begin{aligned}
\min_{\boldsymbol{\alpha}} \quad & \sum_{i=1}^{|\alpha|} (\alpha_i - \alpha_i^0)^2 \\[4pt]
\text{s.t.} \quad & A^{\text{tri}} \boldsymbol{\alpha} = \pi \mathbf{1}_{|F|} \quad &\text{（三角形约束，$|F|$ 个）} \\[4pt]
& A^{\text{vert}} \boldsymbol{\alpha} = 2\pi \mathbf{1}_{|V_{\text{int}}|} \quad &\text{（顶点约束，$|V_{\text{int}}|$ 个）} \\[4pt]
& \prod_{i=1}^{d_v} \frac{\sin \beta_i}{\sin \gamma_i} = 1 \quad \forall v \in V_{\text{int}} \quad &\text{（正弦积约束，$|V_{\text{int}}|$ 个）}
\end{aligned}
$$

## 解析函数视角

复变函数是一门从18世纪就诞生的数学科目，早已发展成熟，关于共形几何的理论也有十分丰富的理论成果。在复变函数理论中，复变函数即平面图形的变形，而其中的解析函数就是保角的共形映射。关于解析函数最基础也是最重要的一个结论就是Cauchy-Riemann(C.R.)公式。

![image-20260610102606195](https://lgximgs.oss-cn-beijing.aliyuncs.com/images/image-20260610102606195.png)

### Cauchy-Riemann(C.R.)公式

设 $f:\mathbb{C}\to\mathbb{C}$，$f$ 为共形映射当且仅当对一切切向量 $X$ 与复数 $z$，微分满足

$$
df(zX)=z\,df(X)
$$

即在映射前或后对切向量做旋转/伸缩（乘以 $z$），结果一致。

![image-20260529124815365](https://lgximgs.oss-cn-beijing.aliyuncs.com/images/image-20260529124815365.png)

将 $f=u+iv$ 写出，上式等价于 **Cauchy–Riemann 方程**：

$$
\frac{\partial u}{\partial x}=\frac{\partial v}{\partial y},\qquad
\frac{\partial u}{\partial y}=-\frac{\partial v}{\partial x}
$$

从复可微性到 CR 方程的推导见《共形映射初探-解析函数观点》**附录**。

### 黎曼映射定理

理论上的共形映射的**存在性由黎曼映射定理保证**

<img src="https://lgximgs.oss-cn-beijing.aliyuncs.com/images/image-20260608173236184.png" alt="image-20260608173236184" style="zoom:50%;" />

**黎曼映照定理 (Riemann Mapping Theorem)**：设 $U\subsetneq\mathbb{C}$ 为单连通真子集，则存在共形映射

$$
f:U\xrightarrow{\text{共形}}\mathbb{D},\quad \mathbb{D}=\{z\in\mathbb{C}\mid|z|<1\}
$$

**唯一性（模 Möbius 变换）**：若 $f_1,f_2$ 均为 $U\to\mathbb{D}$ 的共形映射，则存在 $a\in\mathbb{D}$、$\theta\in\mathbb{R}$ 使
$$
f_2(z)=e^{i\theta}\,\frac{f_1(z)-a}{1-\bar{a}\,f_1(z)}
$$

单位圆盘到自身的共形自映射恰为上述 **Möbius 变换**（三个自由度：反演中心 $a$ 与旋转 $\theta$）。二维 **Möbius 变换**是保定向、将圆/直线映为圆/直线的共形映射，代数形式为

$$
z\mapsto \frac{az+b}{cz+d},\qquad ad-bc\neq 0
$$

<img src="https://lgximgs.oss-cn-beijing.aliyuncs.com/images/image-20260608173425840.png" alt="image-20260608173425840" style="zoom:50%;" />

Möbius 变换的可视化可查看这个网站  [Möbius transformations revealed](https://www-users.cse.umn.edu/~arnold/moebius/)。

![image-20260529125202435](https://lgximgs.oss-cn-beijing.aliyuncs.com/images/image-20260529125202435.png)

### 从曲面到平面的共形映射

如图将一个三维人脸曲面映射到平面圆盘上：任意两条相交曲线映到平面后，交点处夹角保持不变。

![](https://lgximgs.oss-cn-beijing.aliyuncs.com/images/image-20250312183649382.png)

它能最大程度地维持几何细节的形状不失真。

![](https://lgximgs.oss-cn-beijing.aliyuncs.com/images/image-20260529125522229.png)

## 共形模与 Teichmüller 空间

黎曼映射定理保证**单连通**区域到圆盘的共形映射存在，但曲面拓扑更一般时（带孔、高亏格），共形结构不能由边界形状单独决定，而由**共形模**与 **Teichmüller 空间**中的坐标描述。二者回答的是：在固定拓扑的前提下，「有多少种本质不同的共形几何？」

### 共形模 (Conformal Module)

**共形模**是区域在共形变换下不变的量，只反映共形几何的「厚薄」「宽窄」，与欧氏长度、面积无关。

**圆环（annulus）**。设 $A=\{z\in\mathbb{C}: r<|z|<R\}$，$0<r<R$。任意共形映射 $A\to A'$ 只能将内外边界同时伸缩同一倍数，故比值 $R/r$ 是共形不变量。更常用的**共形模**定义为

$$
\mathrm{mod}(A)=\frac{1}{2\pi}\log\frac{R}{r}>0.
$$

$\mathrm{mod}(A)$ 越大，圆环在共形意义下越「厚」——要把 $A$ 共形压扁，内外边界越难同时贴近。等价地，可用**极值长度 (extremal length)**：连接内外边界的所有曲线中，长度与宽度的最优比值由 $\mathrm{mod}(A)$ 刻画。

**四边形**。任意带四个标记顶点的单连通域 $Q$，存在唯一（模 Möbius）共形映射将 $Q$ 映到矩形 $[0,a]\times[0,1]$。边长比 $a\ge 1$ 即为四边形的共形模，描述「横向比纵向宽多少」。

**与参数化的关系**：UV 展开时若域拓扑为圆环（如圆柱侧面、带一条缝的曲面），内外边界对应值不能任意指定——它们的共形模必须与曲面内禀共形结构一致，否则不存在全局共形映射。孔洞数量、缝的数目进入更一般的模问题。

### Teichmüller 空间

固定一张可定向闭曲面 $S$（或带标记边界/穿孔的曲面），其上的**共形结构**是使 $S$ 局部像平面的复图册；若存在共形同胚 $f:S\to S'$，则两结构视为同一类。

**Teichmüller 空间** $\mathcal{T}(S)$ 定义为

$$
\mathcal{T}(S)=\{\text{$S$ 上的共形结构}\}/\sim\text{（共形同胚，固定标记点/边界）}.
$$

直观上，$\mathcal{T}(S)$ 是「在给定拓扑上，所有可能共形几何」的参数空间。

| 曲面 | Teichmüller 空间（实维数） | 说明 |
|------|---------------------------|------|
| 球面 $g=0$ | $0$ 维（单点） | 黎曼球，共形类唯一 |
| 环面 $g=1$ | $2$ 维 | 同胚于 $\mathbb{H}/\mathrm{PSL}(2,\mathbb{Z})$；模参数 $\tau\in\mathbb{H}$（见《Ricci 流与参数化》） |
| 闭曲面 $g\ge 2$ | $6g-6$ 维 | 万有覆盖为上半平面 $\mathbb{H}$，Fuchsian 群商 |

**模空间 (Moduli space)** $\mathcal{M}(S)=\mathcal{T}(S)/\mathrm{MCG}(S)$：在 Teichmüller 空间上再对**映射类群** $\mathrm{MCG}(S)$（保向同胚的同痕类）取商，去掉标记后「几何上不可区分」的共形结构合并为一点。

**与参数化的关系**：

- 每次把曲面共形映到平面（圆盘、矩形、圆环等），实质是在 $\mathcal{T}(S)$ 中选一个代表，并加上**边界条件**（固定边界映射相当于额外约束）。
- **圆填充**、离散共形因子（CETM）等可视为在三角网格上近似一个 Teichmüller 点；不同初始圆半径或边界数据对应模空间中不同方向。
- 高亏格曲面参数化常需指定 **cone 奇点 / 割缝**（分支覆盖、多片 UV），正是在补全拓扑与标记信息后，才能在有限维模空间中唯一确定共形结构。

共形参数化因此不仅是「解一个 Laplace 方程」，而是在固定拓扑下选取（或优化）一点共形几何，再将其展平到参数域——共形模与 Teichmüller 理论给出了「有多少自由度、哪些量必须保持不变」的语言。

---

## 圆形填充近似共形变换

圆是共形几何的基本构建单元，**圆填充可以用来近似共形映射（Thurston）**，共形映射保持角度和圆不变：将**圆映射为圆**且保持两个圆之间**相切关系或者相交角度**不变

<img src="https://lgximgs.oss-cn-beijing.aliyuncs.com/images/Screenshot%202026-05-07%20at%2020.35.03.png" alt="image-20260116183624930" style="zoom:50%;" />



**Circle Packing（圆填充）** 是指在平面（或曲面）上排布一组圆，使得相邻的圆**外切（相切不重叠）**，并且它们的切点关系与给定的图的邻接关系完全一致。

<img src="https://lgximgs.oss-cn-beijing.aliyuncs.com/images/image-20260610100117034.png" alt="image-20260610100117034" style="zoom: 67%;" />

对于一个三角网格而言，每个顶点对应一个圆，若两顶点之间有边相连，则对应的两个圆相切。这样便建立了**图的组合结构**与**平面几何配置**之间的对应。三角网格与圆填充对应的存在性由**Koebe–Andreev–Thurston（KAT）定理**保证，Stephenson 等人实现了三角网格上实现 Circle Packing 的算法。

![](https://lgximgs.oss-cn-beijing.aliyuncs.com/images/image-20251204102324155.png)

可以认为圆填充是一种曲面的共形结构，而我们将曲面保角展开到平面上，可以认为是在平面上重建这个共形结构。

## 共形参数化特点

共形参数化在**数值计算上往往都是比较简单的**，共形参数化只要求保角，离散后往往化为 Laplace 方程或线性最小二乘（LSCM、调和映射、ABF++ 等），一次线性求解即可。几何优化（BDHM/Lipman、ARAP 等）还需控制扭曲上界与无翻转，约束含 Jacobian 比值与范数，通常需 **SOCP** 或交替非线性迭代——共形计算更轻，几何优化换取对畸变的显式控制。

调和映射与共形映射这两个概念容易混淆，调和映射它最小化拉伸(Dirichlet 能量)，但不一定保持角度即不一定是共形的。而共形映射却一定是调和的。

**为什么共形映射一定是调和的？**

设 $f: \mathbb{C} \supset \Omega \to \mathbb{C}$ 是一个光滑的共形映射，记为 $f(z) = u(x,y) + i\,v(x,y)$。需要证明 $u$ 和 $v$ 都满足 Laplace 方程 $\Delta u = 0,\ \Delta v = 0$。

在复分析中，共形映射（保角且保定向）等价于**解析函数**，故满足 **Cauchy–Riemann 方程** $u_x=v_y$、$u_y=-v_x$（推导见《共形映射初探-解析函数观点》附录）。

**（1）Cauchy–Riemann → Laplace 方程**

对 $u_x = v_y$ 两边求 $x$ 偏导、对 $u_y = -v_x$ 两边求 $y$ 偏导：

$$
u_{xx} = v_{yx},\qquad u_{yy} = -v_{xy}
$$

若 $f$ 为 $C^2$（实际上解析函数自动光滑），混合偏导可交换 $v_{yx}=v_{xy}$，相加得：

$$
u_{xx} + u_{yy} = v_{yx} - v_{xy} = 0 \quad\Longrightarrow\quad \Delta u = 0
$$

同理对 $v$ 可得 $\Delta v = 0$。因此 $u$ 和 $v$ **均为调和函数**，且由 CR 知两者互为**共轭调和函数**。

**（2）Dirichlet 能量视角**

调和函数 $\Delta u = 0$ 是 Dirichlet 能量 $E_D[u] = \frac{1}{2}\int_\Omega \|\nabla u\|^2$ 的 Euler–Lagrange 方程的解。共形映射 $f = u + i v$ 的总 Dirichlet 能量为：

$$
E_D[f] = \frac{1}{2}\int_\Omega \|\nabla u\|^2 + \|\nabla v\|^2
\stackrel{(CR)}{=} \int_\Omega \|\nabla u\|^2
$$

由于 $u$ 调和意味着 $E_D[u]$ 在固定边界条件下极小——但此处关键不是"极小"而是"critical point"：$u$ 满足 $\delta E_D[u] = 0$ 即 $\Delta u = 0$。

**（3）离散版本**

在三角网格上，类似的结论也成立：若离散映射在每个面局部相似（离散共形/相似变换），则其坐标函数满足离散 Laplace 方程——即满足闭合 1-form（closed）与 co-closed（无散）条件，等价于调和条件。这就是为什么**Tutte 的凸组合嵌入（调和）是共形映射的必要条件**。

<img src="https://lgximgs.oss-cn-beijing.aliyuncs.com/images/image-20260603165819767.png" alt="image-20260603165819767" style="zoom:80%;" />

前面的基础参数化方法我们介绍调和参数化，通过指定边界上每点的像，我们带入Laplace方程求解得到**调和映射**，但这个过程并不保角，边界被"钉死"后，内部为平滑插值，角度不再保持。

![image-20260608173458420](https://lgximgs.oss-cn-beijing.aliyuncs.com/images/image-20260608173458420.png)



在三角网格上进行共形映射计算有两条路线，一种思路Discretized（离散化）是将连续的共形几何理论通过有限元方法直接离散化，另外一种思路Discrete（离散）是直接基于三角网格(或单纯形)上建立新的共形几何理论

| | Discretized（离散化） | Discrete（离散） |
|---|----------------------|-----------------|
| 保什么 | 细化极限下保角（如角度） | 粗网格上**精确**保某离散量（如 length cross ratio） |
| 视角 | 有限元 / 科学计算 | 离散微分几何 (DDG) |
| 计算 | 多为线性问题 | 可能需凸优化 |
| 代表算法 | LSCM、调和映射等 | 圆填充、CETM、inversive distance |

共形等价在连续和离散上的定义

![](https://lgximgs.oss-cn-beijing.aliyuncs.com/images/image-20260529130558905.png)

