### **联系方式**

李国雄 18813021539

邮箱 lixiongguox@163.com

### **教育背景**

- **2014.9 – 2016.7**　北京邮电大学　软件工程（硕士）
- **2010.9 – 2014.7**　安徽大学　数学与应用数学（学士）

> 英语能力：CET-6，具备熟练检索与复现英文技术文献的能力

------

### **专业技能**

- **图形与视觉算法**：熟悉图形渲染、几何处理等核心算法，具备算法复现能力
- **全栈开发能力:**
  - 前端：Android（Camera2/OpenGL）、Web（three.js/WebAssembly/Unity WebGL）
  - 后端：Spring Boot（RESTful API / 微服务 / MySQL / Redis / gRPC）
- **高性能计算**：熟练使用 CUDA、OpenMP、AVX2、Halide 对 CV/图形算子进行并行加速
- **工程素养**：具备扎实的软件工程意识，持有软考中级“软件设计师”证书；熟悉 Git、CI/CD、性能分析工具（Profiler）

------

### **工作与项目经历**

#### **松山湖阿秒激光**　｜　数字孪生与信息管理系统负责人　｜　2024.8 – 至今

**边缘智能 / 数字孪生 / 全栈工业软件开发**

- 主导激光器数字孪生平台的端到端架构设计与开发，实现设备状态可视化、工艺过程监控与远程运维一体化。
  - 前端系统：基于 **Vue 3 + TypeScript** 构建响应式管理界面，集成 **three.js** 实现高保真 3D 激光器模型渲染与实时数据绑定（温度、功率、气流等）；支持 WebGL 性能优化，确保在中低端工控机上流畅运行。
  - 多路视频接入与边缘推理：
    - 采用 **瑞芯微 RK3588 四核 ARM + NPU** 边缘计算平台，部署 **8 路 1080p@30fps 视频流**；
    - 视频解码基于 **Rockchip MPP**（Media Process Platform），通过 **VPU 硬件加速** 实现低功耗、高吞吐解码（H.264/H.265）；
    - 部署轻量化视觉检测模型（YOLOv8s / EfficientDet-Lite），利用 **RKNN Toolkit** 将模型转换为 NPU 友好格式，在 RK3588 上实现 **8 路并发实时检测**（<100ms/帧），用于监控激光器腔体异物、冷却管泄漏、机械位移等异常。
  - 后端服务：基于Spring Boot 3 + Spring Cloud Alibaba构建微服务架构，提供：
    - 设备元数据管理（激光器型号、传感器拓扑）
    - 视频流调度与告警事件订阅（WebSocket + RabbitMQ）
    - 检测结果持久化（PostgreSQL + TimescaleDB 时序数据）
    - RESTful API 供前端调用 3D 场景状态与告警记录
  - 系统集成：打通 PLC（Modbus TCP）、摄像头（RTSP）、边缘 AI 盒与云端管理平台，构建“感知-分析-可视化-告警”闭环，支撑激光器无人化值守与预测性维护

------

#### **华为技术有限公司（东莞）**　｜　制造部 · 机器视觉实验室　｜　2021.9 – 2024.7

- **自研工业视觉平台**（对标 Halcon）
  - 作为重要成员参与核心 CV 算子开发：包括亚像素级 **模板匹配**（NCC/FFT 加速）、**圆/线/边缘检测**（基于梯度聚类与 RANSAC）、Blob 分析 等，满足 SMT 与 AOI 检测需求；
  - 实施多层级并行优化：
    - GPU 层：使用 **CUDA** 重构卷积、形态学等算子；
    - CPU 层：通过 **OpenMP + AVX2 指令集** 优化密集计算；
    - NPU 层：基于 **Halide 前端**，针对昇腾芯片特性生成高效算子代码，实现跨硬件部署；
  - 开发高性能 2D/3D 渲染控件（基于 OpenGL）：
    - 支持 **超大 PCB 板**（>10K × 10K pixels），借鉴游戏引擎 **虚拟纹理**（Virtual Texture）技术，实现局部区域高清放大与流畅漫游；
    - 集成 **矢量绘图板式 ROI 编辑器**，支持多边形、贝塞尔曲线、环形等复杂测量区域定义；
    - 实现 **2D 尺寸测量、3D 高度差分析、共面度检测** 等工业标准功能，精度达 ±5μm。
- **工程落地与产线协同**
  - 与制造工艺工程师紧密合作，将算法模块集成至 **华为自研 AOI 设备**，完成在手机主板、光模块等产线的试点部署；
  - 建立 **算法-硬件-软件闭环验证机制**，确保系统在 24/7 连续运行下的稳定性与鲁棒性；
  - 输出 **标准化 API 与 SDK**，供上层 MES 系统调用，推动视觉能力在制造部多个工厂复用。

#### **华为技术有限公司（深圳）**　｜　河图技术开发部　｜　2021.9 – 2024.7

**3D 视觉算法 / 高性能 C++ 开发**

- **高精度三维重建系统研发与优化**
  - 基于 **OpenMVS + CasMVSNet** 构建端到端重建 pipeline，针对产线复杂光照与遮挡场景进行定制化改进；
  - 引入 **前景分割神经网络（U²-Net）预处理输入图像**，有效剔除背景干扰，提升稀疏点云质量；
  - 改进特征提取模块：设计 **多尺度级联 U-Net 结构**，增强纹理缺失区域的匹配鲁棒性；
  - 利用 **神经架构搜索**（NAS），自动探索轻量化 backbone，在保持重建精度（RMSE < 0.3mm）前提下，推理速度提升 35%；
  - 实现 **CUDA 并行加速的点云融合与泊松重建**，单帧处理耗时从 8s 降至 5s；
  - 开发 **交互式 OpenGL 可视化工具**，引导操作员按最优路径采集图像，显著提升重建成功率。
- **工业视觉平台开发**（对标 Halcon）
  - 实现模板匹配、找圆/找线等核心 CV 算子，基于 CUDA/OpenMP/AVX2 进行多级并行优化；
  - 使用 Halide 重构插值、滤波、形态学等算子，适配 NPU 硬件特性；
  - 开发高性能 OpenGL 渲染控件，支持大型 PCB 板局部高清放大（借鉴虚拟纹理技术）、矢量 ROI 编辑、2D/3D 测量等功能。

------

#### **商汤科技有限公司**　｜　SenseAR 开发团队　｜　2019.8 – 2021.2

**C++ / Android / Spring Boot 全栈开发**

- **特效相机 SDK 开发**（客户：映客直播、B612）
  - 移植 MLS（Moving Least Squares）图像变形算法，实现瘦脸、大眼等实时美颜特效；
  - 研发基于 SMPL 的人体姿态检测与重建方案，完成 Android 端部署与渲染优化；
  - 跟踪 NeRF/3DGS 等前沿技术，开发三维重建算法原型。
- **UGC(3D)内容生成编辑器**（PC 端，Qt）
  - 作为开发组长，带领 3 人团队开发类 Blender 的 UGC 编辑器；
  - 实现可拖拽窗口布局、场景资源序列化、可视化脚本编辑（Scratch 风格）、资源加载优化等核心功能。
- **WebAR 室内导航系统**
  - **后端服务开发**：基于 **Spring Boot 构建微服务架构**，提供 AR 地图管理、用户会话维护、SLAM/VPS 算法调度等核心能力；
  - **API 与集成**：设计 RESTful 接口供 Web 前端（three.js）调用，封装 SLAM/VPS 模块为 gRPC 服务，实现低延迟坐标下发；
  - **数据与性能**：使用 MySQL 存储场景配置，Redis 缓存热点数据，支撑高并发访问；
  - **多端协同**：统一 WGS84 → ENU → 局部 AR 坐标系转换逻辑，确保 three.js 与移动端渲染一致性；
  - **前端集成**：通过 Unity + ARFoundation 完成技术验证，利用 WebAssembly 集成自研算法，解决 iOS/Android 浏览器兼容性问题。

------

#### **北京掌趣科技股份有限公司**　｜　引擎开发组　｜　2016.10 – 2019.6

**Unity 引擎扩展 / 图形渲染 / 高性能游戏玩法开发**

- **自研网格建模与处理工具链**
  - 基于 Unity Editor 深度二次开发，构建面向美术与策划的**一体化网格编辑平台**；
  - 实现 **2D 多边形布尔运算与约束 Delaunay 三角剖分**，支持复杂地形/UI 路径生成；
  - 开发 **3D 网格参数化、简化、变形（Laplacian/ARAP）等几何处理算法**，用于角色换装、场景 LOD 生成等业务场景；
  - 工具链显著提升美术资源生产效率，减少外包返工率约 30%。
- **多人网络游戏**（2017年上线MMORPG《龙族世界》）
  - 主导 C# 全栈玩法系统开发（Unity 客户端 + .NET Core 服务端），支持 **50+ 并发玩家的高频交互**（如实时对战、协作解谜）；
  - 设计 **双同步架构**：关键状态采用**帧同步**保障公平性，非核心逻辑采用**状态同步**降低带宽；
  - 自研 **MVC UI 框架**，仿 Android 开发范式对 UGUI 进行封装，实现“美术拼界面 + 程序绑定逻辑”的高效协作流程，UI 迭代周期大幅缩短
- **定制化渲染管线与特效系统**
  - 设计混合光照方案：静态环境依赖 **Lightmap**，动态主角采用 **类《Fortress II》卡通渲染（Toon Shading）**；
  - 复现并优化 **ShadowMap 动态阴影**，结合级联（CSM）与 PCF 提升质量，适配移动端性能约束；
  - 引入 **Decal 投影系统**，实现地面光斑、血迹、水渍等静态交互痕迹，增强场景沉浸感；
  - 编写 **Shader Graph + HLSL 特效着色器**，配合美术实现粒子交互（如草叶扰动、雪地脚印、水面涟漪）。
- **全链路性能优化与工程看护**
  - 建立 **常态化 Profiler 监控机制**，定位 CPU/GPU 瓶颈（如 DrawCall 过高、Overdraw 严重）；
  - 通过 **静态合批（Static Batching）修复、Shader 变体裁剪、纹理压缩策略优化**，使中低端机型帧率提升 15–25%；
  - 分析 SRP Batcher 失败原因，重构材质与 Shader，推动团队渲染规范落地。

------
### **学生时代项目经历**

#### **RoboCup 2D 仿真机器人足球**　｜　核心算法开发者　｜　2012.3 – 2013.6

**强化学习 / 多智能体协作 / 策略优化**

- 基于 **Helios Base** 开源框架，主导设计并实现多层级强化学习策略系统，参与中国机器人大赛 RoboCup 2D 仿真组（全国前八）。

- 分层决策架构 ：
  - **高层策略**：采用 **Q-learning + 经验回放** 学习攻防转换、角色分配等宏观行为；
  - **底层动作**：基于 **状态离散化 + 查表式策略** 实现高速跑位、拦截、射门等实时控制（决策频率 10Hz）；

- 引入 **对手建模机制**，通过在线聚类识别对方战术模式，动态调整防守阵型；

- 设计 **自对弈训练 pipeline**，利用多进程并行模拟数百万局比赛，显著提升策略泛化能力；

- 项目成果验证了**强化学习在复杂、部分可观测、非平稳多智能体环境中的可行性**，为后续工业级 AI 系统开发奠定算法基础。

#### **计算机围棋引擎开发**（基于蒙特卡洛树搜索与策略网络）　｜　北邮九鼎计算机围棋实验室　｜　2014.9 – 2015.12

**深度强化学习 / MCTS / 神经网络策略**

- 在 AlphaGo 论文发表前夕，独立复现并改进 **蒙特卡洛树搜索**（MCTS），构建支持 9×9 和 19×19 棋盘的围棋 AI 引擎。

- 混合策略设计：
  - 初期使用 **手工特征 + 逻辑回归** 构建快速走子策略（Rollout Policy）；
  - 后期引入 **浅层 CNN**（3 层卷积 + 全连接）作为策略网络，通过 **监督学习**（KGS 数据集）预训练；
  - 探索 **策略梯度微调**（REINFORCE）提升对弈胜率；

- 实现 **多线程 MCTS 并行扩展**，单机 8 核下每秒可模拟 8,000+ 局面；

- 引擎在本地测试中可稳定战胜 GNU Go（业余 5–7 级水平），验证了**数据驱动策略优于纯规则系统**；

- 该项目深入锻炼了**从理论复现 → 算法改进 → 系统集成 → 性能调优**的全栈 AI 开发能力，是早期强化学习工程化的重要实践。


------
### **证书与资质**

- 软考中级：软件设计师（国家职业资格证书）
- 大学英语六级（CET-6）

------
### **自我评价**

具备扎实的数学与软件工程基础，8年深耕**计算机图形学、3D视觉与高性能计算**领域，兼具**算法研发、系统架构与工程落地**能力。

熟悉从学术原型（NeRF/3DGS/SMPL）到工业部署（CUDA/Halide/昇腾NPU）的完整技术链条，参与了多个高复杂度项目：包括游戏引擎工具链、AR内容平台、WebAR导航系统、工业级视觉检测平台及数字孪生系统。

工程意识强，注重代码质量、性能优化与跨团队协作，能在**科研探索与产线需求之间高效平衡**。追求技术深度的同时，始终以**解决实际问题、创造业务价值**为导向。