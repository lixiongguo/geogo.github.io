

Atari的Pong游戏

在游戏中控制右边的球拍，你与电脑控制的左边的球拍竞争。

![image-20250228193214531](https://lgximgs.oss-cn-beijing.aliyuncs.com/images/image-20250228193214531.png)



对这个问题的MDP建模

利用卷积神经网络处理图像输入

面对非结构化的数据（如图像），那就需要用到深度神经网络(如CNN,RNN等)来提取特征进行学习。

有了CNN之后可以认为AI是有了自己的”眼睛“了。CNN网络结构设计同时为了整合Temporal信息将多帧堆叠在一起

![在这里插入图片描述](https://lgximgs.oss-cn-beijing.aliyuncs.com/images/54e46fe019ceb8449d865d34bb020a84.png)



通过将卷积层的输出可视化出来，我们可以看到如下图景，CNN实际上是将高维数据的图像提取特征后浓缩为了低维的特征给后面的处理做准备



Input : 初始化策略Pi(s)
Output : 最优策略
Repeat(多轮迭代)
	策略Pi(s)从状态S0开始生成一个回合的轨迹episode=[(s0,a0,r0),(s1,a1,r1)....(st,at,rt)]
	初始化回报G=0
	for (s,a,r) in reverse(episode):
		G = r + \gamma * G 
		Q(s,a) = Q(s) + \alpha*(G - Q(s,a))
	策略提升Pi(s) = Onehot(index == argmaxQ(s,a))



附录A: 马尔可夫决策问题（Markov Dicision Progress)

动态规划与Bellman期望方程

附录2：折算因子的影响可以这么估计$1+\gamma + \gamma^2 + ... = 1/(1-\gamma)$,所以如果设$\gamma=0.99$可以忽略100个时间步后的奖励

![image-20250303111405037](..\..\..\imgs\image-20250303111405037.png)

0.99^50 = 0.60,0.98^20=0.66

2SARSA与Q-Learning的伪代码

```
初始化策略Pi(s) = Uniform in {a1,a2,a3,....}
Repeat(多轮迭代)
	for (s,r,a) in episode:
		G = r + gamma * G 
		Q(s,a) = Q(s) + l*(G - Q(s,a))
	策略提升Pi(s) = Onehot(index == argmaxQ(s,a))
```

![image-20250301162015719](..\..\..\imgs\image-20250301162015719.png)



