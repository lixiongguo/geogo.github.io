### 博客

  LSCM 内存问题
  构造三角面反转的案例
  ARAP没有用
  自动切割算法与全局参数化
  MongeAmpere与Euclid代码集成
  计算几何界面改好
  四边形网格化MIQ的实现
  构造三角面翻转的反例


  对天玺三维重建效果的实现
  将游记补全，先把照片下载下来


## 问题

### CETM 的能量泛函与 Circle Patterns 的能量泛函形式相似但又有差异，这些奇怪的表达式是如何推导出来的？它们与双曲几何有何联系？

### QuadCover中使用分支覆盖具体作用又是什么呢？

### 四边形网格化与全局参数化的关系，两者可以相互导出吗？

四边形网格化（Quad Meshing）的输出不仅仅是一对 UV 坐标，而是一个完整的四边形网格拓扑结构。
虽然 UV 坐标在算法过程中起到了至关重要的作用，但“UV 坐标”和“最终的四边形网格”是两个不同阶段的数据产物。为了帮你彻底理清这个关系，我们可以从输入、中间过程、最终输出三个维度来详细拆解：
🗺️ 中间产物：UV 坐标（参数化）
在四边形网格化（特别是基于参数化的方法，如 libigl 中的 MIQ 算法）的过程中，算法的第一步确实是计算 UV 坐标。
作用：算法试图找到一个从 3D 表面到 2D 平面的映射（参数化）。在这个 2D 平面（UV 空间）上，算法会尝试让网格线（等值线）尽可能正交、对齐，形成类似“棋盘格”的布局。
局限性：此时的 UV 坐标可能包含重叠、扭曲，或者仅仅是定义在原始三角形顶点上的属性。它们本身还不是四边形网格，只是生成网格的“蓝图”或“引导场”。
🕸️ 最终输出：四边形网格（拓扑）
算法的第二步是利用上述的 UV 坐标来构建新的几何体。
提取：算法会追踪 UV 空间中的整数等值线（即 u=整数, v=整数 的线）。这些线在 UV 平面上围成了一个个正方形。
映射回 3D：算法将这些 UV 平面上的正方形“映射”回 3D 模型表面。
生成新数据：
新的顶点：原来的三角形顶点可能被丢弃或重新采样，生成一组全新的 3D 坐标点。
新的面：数据结构不再是三角形（3个顶点索引），而是四边形（4个顶点索引）。

### 焦散透镜

将stl转化为Step文件,并跟那个同事介绍的朋友联系加工

为什么Poisson重建的效果不如最有传输的？实验证明
3.Fusion360 CNC加工 Blender仿真的进一步测试
4.parabolic的计算

光子朗外包制作
