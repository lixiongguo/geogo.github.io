# 2026-04-08 / 2026-04-09 工作日志

## 围棋死活题页面
- 创建了 `tsumego.html` 页面（`/tsumego/`），包含：
  - 36 道 5k 级别围棋死活题题库
  - Canvas 绘制木纹风格围棋棋盘
  - 交互式落子判断（正确/错误反馈）
  - 每日自动轮换题目（基于日期计算索引）
  - 提示、答案、重置功能
  - localStorage 记录做题统计
- 在侧边栏 `_includes/sidebar.html` 添加了"围棋死活题"导航链接

## Jekyll 构建问题修复
- 修复了多篇文章中 `{{\partial` 被 Liquid 误解析的问题（用 `` 包裹）
- 涉及文件：
  - `曲面参数化2-分片线性映射.md`
  - `曲面参数化3-几个基础的算法.md`  
  - `暂存-LSCM算法.md`
- 修复了 `imgs/` 目录下文件名含冒号导致的编码问题（重命名文件）
- 以 `arch -arm64` 模式安装了 arm64 版本的 `bigdecimal` gem

## Ruby 环境问题（待解决）
- 用户 Mac 为 Apple M4 (arm64)，但终端在 Rosetta (x86_64) 模式运行
- 需要以 `arch -arm64 rbenv install 3.3.6` 安装 arm64 原生 Ruby
- 用户需手动在终端执行该命令（编译时间较长）
- Jekyll 构建编码问题已解决：使用 `Encoding.default_external=Encoding::UTF_8;Encoding.default_internal=Encoding::UTF_8` 前缀启动

## UV 动画页面
- 创建了 `uv-animation.html`，包含 4 种 UV 参数化动画效果：
  1. 扭曲波纹（彩虹波浪顶点变形 + 彩色渐变）
  2. 呼吸脉冲（同心圆扩散 + 呼吸缩放）
  3. 涡旋场（螺旋色带 + 扭曲变形）
  4. 棋盘渐变（动态棋盘 + 色相旋转）
- 使用 Three.js ShaderMaterial 实现，支持 OrbitControls 交互
- 修改了 Gallery 导航链接，点击时同时打开 caustics 和 uv-animation 两个窗口
