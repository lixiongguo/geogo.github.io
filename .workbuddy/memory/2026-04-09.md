# 2026-04-08 / 2026-04-09 工作日志

## 围棋死活题页面
- 创建了 `tsumego.html` 页面（`/tsumego/`），包含：
  - 36 道 5k 级别围棋死活题题库
  - Canvas 绘制木纹风格围棋棋盘
  - 交互式落子判断（正确/错误反馈）
  - 每日自动轮换题目（基于日期计算索引）
  - 提示、答案、重置功能
  - localStorage 记录做题统计
- 在侧边栏 `_includes/sidebar.html` 添加了"围棋死活题"导航链接

## Jekyll 构建问题修复
- 修复了多篇文章中 `{{\partial` 被 Liquid 误解析的问题（用 `{% raw %}` 包裹）
- 涉及文件：
  - `曲面参数化2-分片线性映射.md`
  - `曲面参数化3-几个基础的算法.md`  
  - `暂存-LSCM算法.md`
- 修复了 `imgs/` 目录下文件名含冒号导致的编码问题（重命名文件）
- 以 `arch -arm64` 模式安装了 arm64 版本的 `bigdecimal` gem

## Ruby 环境问题（待解决）
- 用户 Mac 为 Apple M4 (arm64)，但终端在 Rosetta (x86_64) 模式运行
- 需要以 `arch -arm64 rbenv install 3.3.6` 安装 arm64 原生 Ruby
- 用户需手动在终端执行该命令（编译时间较长）
- Jekyll 构建仍有编码相关错误，可能与 Ruby 版本/编码配置有关
